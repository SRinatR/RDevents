// The executable development seed lives in ../../prisma/seed.ts because Prisma
// runs seeds from the prisma directory. This shim prevents stale src/seed code
// from drifting from the active schema.
export {};
