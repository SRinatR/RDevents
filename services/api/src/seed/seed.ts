// The executable demo seed lives in ../../prisma/seed.ts because Prisma runs
// seeds from the prisma directory. This file is intentionally kept as a shim
// so stale src/seed code cannot drift from the active schema.
export {};
