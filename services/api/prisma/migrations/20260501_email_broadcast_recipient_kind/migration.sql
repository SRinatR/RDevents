DO $$ BEGIN
  CREATE TYPE "EmailRecipientKind" AS ENUM ('USER','EVENT_MEMBER','TEAM_MEMBER','PREFILL_CONTACT','MANUAL_EMAIL');
EXCEPTION
  WHEN duplicate_object THEN null;
END $$;

ALTER TABLE "email_broadcast_recipients"
  ADD COLUMN IF NOT EXISTS "recipientKind" "EmailRecipientKind" NOT NULL DEFAULT 'USER',
  ADD COLUMN IF NOT EXISTS "eventMemberId" TEXT,
  ADD COLUMN IF NOT EXISTS "teamMemberId" TEXT,
  ADD COLUMN IF NOT EXISTS "prefillContactId" TEXT,
  ADD COLUMN IF NOT EXISTS "manualEmail" TEXT,
  ADD COLUMN IF NOT EXISTS "phone" TEXT,
  ADD COLUMN IF NOT EXISTS "fullName" TEXT,
  ADD COLUMN IF NOT EXISTS "firstName" TEXT,
  ADD COLUMN IF NOT EXISTS "lastName" TEXT,
  ADD COLUMN IF NOT EXISTS "avatarUrl" TEXT,
  ADD COLUMN IF NOT EXISTS "eventId" TEXT,
  ADD COLUMN IF NOT EXISTS "teamId" TEXT,
  ADD COLUMN IF NOT EXISTS "teamName" TEXT,
  ADD COLUMN IF NOT EXISTS "teamCode" TEXT,
  ADD COLUMN IF NOT EXISTS "role" TEXT,
  ADD COLUMN IF NOT EXISTS "skipReasonCode" TEXT,
  ADD COLUMN IF NOT EXISTS "skipReasonText" TEXT;
